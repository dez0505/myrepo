import React, { Component } from 'react'
import PropTypes from 'prop-types'
import { getOptionalBigEvent } from '@/api/topLine'


export class bigEventCase extends Component {
  static propTypes = {
    prop: PropTypes
  }

  render() {
    return (
      <div>
        
      </div>
    )
  }
}
